apiVersion: v1
kind: Service
metadata:
  name: {{ include "web.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "web.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 4173
